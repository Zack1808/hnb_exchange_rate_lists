export const hnbApi = {
  getListData: async (date: string): Promise<Record<string, string>[]> => {
    return [];
  },
  getPeriodData: async (
    fromDate: string,
    toDate: string
  ): Promise<Record<string, string>[]> => {
    return [];
  },
};
